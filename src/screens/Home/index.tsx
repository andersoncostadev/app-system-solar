import React from 'react';

import {Background, Container, IconContainer, Icon} from './styles';

const Home: React.FC = () => {
  return (
    <Background>
      <Container>
        <IconContainer>
          <Icon />
        </IconContainer>
      </Container>
    </Background>
  );
};

export default Home;
